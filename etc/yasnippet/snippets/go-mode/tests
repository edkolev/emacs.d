# -*- mode: snippet -*-
# key: tests
# --

tests := []struct {
	name string$0
}{
	{
		name: "...",
	},
}

for _, tt := range tests {
	t.Run(tt.name, func(t *testing.T) {
		// got, err := `(string-trim-left (treesit-defun-name (treesit-defun-at-point)) "Test_?")`(tt.input)
		// assert.NoError(t, err)
		// assert.Equal(t, tt.want, got)
	})
}